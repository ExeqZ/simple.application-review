{
  "_comment": "Rename this file to <customer-shortname>.json (e.g. contoso.json) and fill in your values.",
  "_authNote": "Auth fields (authMethod, clientId, certificate*, clientSecretFile) are OPTIONAL if config/auth-defaults.json exists. Omit them to inherit global auth (multi-tenant app). Specify them here to override per-tenant.",
  "tenantId": "aaaaaaaa-0000-0000-0000-000000000001",
  "tenantName": "Customer Name - Company Ltd",
  "enabled": true,

  "_perTenantAuthOverride": "Uncomment below ONLY if this tenant uses a different app registration than the global default:",
  "authMethod": "",
  "clientId": "",

  "_certificateOptions": "Use ONE of the following certificate options (only needed for per-tenant override):",
  "certificateThumbprint": "",
  "certificatePath": "",
  "certificatePasswordFile": "",

  "_clientSecretOption": "Only required when authMethod=ClientSecret (per-tenant override):",
  "clientSecretFile": "",

  "settings": {
    "_comment": "Override global defaults for this tenant only. Remove keys to inherit the global default.",
    "inactivityThresholdDays": 180,
    "signInLookbackDays": 30,
    "signInBatchSize": 5,
    "includeDisabledApps": false,
    "includeManagedIdentities": true,
    "includeFirstPartyMicrosoftApps": false
  },

  "logAnalytics": {
    "_comment": "Optional: set enabled=true and supply workspaceId to use Log Analytics for extended sign-in history (up to 365+ days). Requires Diagnostic Settings streaming and Log Analytics Reader RBAC on the workspace.",
    "enabled": false,
    "workspaceId": "",
    "lookbackDays": 365
  }
}
